from setuptools import setup, find_packages

setup(
   name='personapi',
   version='1.0',
   description='A module for ingesting, sorting, and returning person data',
   author='Grant Nicholas',
   author_email='grantnicholas2015{at}u.northwestern.edu',
   packages=find_packages(),
   install_requires=[]
)